/** App branding, in one place. */
export const APP_NAME = "Music Tycoon";
export const APP_DESCRIPTION = "Simulador de Produção Musical";

/**
 * Whether the developer-only options are shown (the "Opções (dev)" tab). They
 * are tooling for building the game — previews, sample data — and are hidden in
 * the released build.
 */
export const DEV_OPTIONS_ENABLED = import.meta.env.DEV;
